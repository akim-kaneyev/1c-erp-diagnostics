[project]
name = "one-c-erp-diagnostics"
version = "0.3.2"
description = "Dynamic evidence-first toolkit and verified companion ecosystem for 1C:ERP diagnostics"
requires-python = ">=3.10"
dependencies = [
  "openpyxl>=3.1",
  "pypdf>=5",
]

[project.optional-dependencies]
artifacts = ["v8unpack==1.2.6"]
sandbox = ["opensandbox==0.1.14"]
