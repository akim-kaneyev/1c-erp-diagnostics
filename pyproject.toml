[project]
name = "one-c-erp-diagnostics"
version = "0.1.2"
description = "Evidence-first toolkit for 1C:ERP incident diagnostics"
requires-python = ">=3.10"
dependencies = [
  "openpyxl>=3.1",
  "pypdf>=5",
]

[project.optional-dependencies]
sandbox = ["opensandbox"]
